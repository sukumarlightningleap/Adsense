import { Building2 } from "lucide-react";

import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function AccountsPage() {
  return (
    <PlaceholderPage
      icon={Building2}
      title="Accounts"
      body="Connect Google Ads customer accounts (direct or via your MCC), see currency / time-zone / GA4 link status at a glance, and manage credentials per profile (test / prod)."
      comingIn="PHASE 2"
    />
  );
}
